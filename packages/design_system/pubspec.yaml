name: design_system
description: "Branding tokens and reusable widgets"
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  localization:
    path: ../localization
