name: networking
description: "Dio configuration and REST clients"
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  localization:
    path: ../localization
  dio: ^5.9.0
  retrofit: ^4.7.2
  get_it: ^8.2.0
  injectable: ^2.5.1
