name: onboarding
description: "Onboarding flow feature"
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../core
  design_system:
    path: ../../design_system
  localization:
    path: ../../localization
  smooth_page_indicator: ^2.0.1
